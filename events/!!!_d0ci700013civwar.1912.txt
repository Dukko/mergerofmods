namespace = civwar
country_event = {
    id = civwar.1912
    hide_window = yes
    is_triggered_only = yes
    immediate = {
        if = {
            limit = {
                any_pop_faction = {
                    is_a_faction = yes
                    exists = leader
                    is_pop_faction_type = xenoist
                    faction_approval < 0.6
                    support >= 0.2
                }
            }
            random_pop_faction = {
                limit = {
                    is_a_faction = yes
                    exists = leader
                    is_pop_faction_type = xenoist
                    faction_approval < 0.6
                    support >= 0.2
                }
                set_pop_faction_flag = civil_war_faction_xenoist
                leader = {
                    set_leader_flag = xenoist_faction_leader
                }
            }
        }
    }
}
