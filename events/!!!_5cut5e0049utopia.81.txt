namespace = utopia
country_event = {
    id = utopia.81
    hide_window = yes
    is_triggered_only = yes
    trigger = {
        FROM = {
            is_megastructure_type = think_tank_1
        }
    }
    immediate = {
        FROMFROM = {
            save_event_target_as = think_tank_system
        }
        every_relation = {
            limit = {
                is_ai = no
                has_communications = root
                OR = {
                    has_intel_level = {
                        who = root
                        category = economy
                        level >= 1
                    }
                    has_intel_level = {
                        who = root
                        category = technology
                        level >= 1
                    }
                }
            }
            country_event = {
                id = utopia.82
            }
        }
        if = {
            limit = {
                NOT = {
                    has_country_flag = started_first_think_tank
                }
            }
            set_country_flag = started_first_think_tank
        }
        else_if = {
            limit = {
                has_country_flag = started_first_think_tank
            }
            set_country_flag = started_first_think_tank_2
        }
    }
}
