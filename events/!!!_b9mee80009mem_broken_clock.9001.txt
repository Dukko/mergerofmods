namespace = mem_broken_clock
country_event = {
    id = mem_broken_clock.9001
    hide_window = yes
    is_triggered_only = yes
    trigger = {
        is_at_war = no
    }
    immediate = {
        random_neighbor_country = {
            limit = {
                is_country_type = default
            }
            save_event_target_as = mem_broken_clock_war_target
        }
        IF = {
            limit = {
                exists = event_target:mem_broken_clock_war_target
            }
            IF = {
                limit = {
                    has_authority = auth_hive_mind
                }
                declare_war = {
                    target = event_target:mem_broken_clock_war_target
                    attacker_war_goal = wg_absorption
                }
            }
            ELSE = {
                declare_war = {
                    target = event_target:mem_broken_clock_war_target
                    attacker_war_goal = wg_cleansing
                }
            }
        }
    }
}
