namespace = action
country_event = {
    id = action.19
    hide_window = yes
    is_triggered_only = yes
    trigger = {
        is_country_type = default
    }
    immediate = {
        if = {
            limit = {
                NOR = {
                    exists = overlord
                    AND = {
                        exists = overlord
                        overlord = {
                            is_same_value = from
                        }
                    }
                }
            }
            add_opinion_modifier = {
                modifier = opinion_new_contact
                who = from
            }
            from = {
                add_opinion_modifier = {
                    modifier = opinion_new_contact
                    who = root
                }
            }
        }
    }
}