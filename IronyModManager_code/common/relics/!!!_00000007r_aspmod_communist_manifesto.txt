r_aspmod_communist_manifesto = {
    activation_duration = 3600
    portrait = "GFX_relic_communist_manifesto"
    sound = "relic_activation_aspmod_communist_manifesto"
    resources = {
        category = relics
        cost = {
            influence = 200
        }
    }
    ai_weight = {
        weight = 100
    }
    triggered_country_modifier = {
        potential = {
            always = yes
        }
        country_base_minerals_produces_add = 15
        country_base_energy_produces_add = 15
    }
    score = 2500
    active_effect = {
        add_modifier = {
            modifier = aspmod_communist_manifesto_relic_activated
            days = 3600
        }
        custom_tooltip = relic_triumph_cooldown
        hidden_effect = {
            add_modifier = {
                modifier = "relic_activation_cooldown"
                days = 3600
            }
        }
    }
    possible = {
        custom_tooltip = {
            fail_text = "requires_relic_no_cooldown"
            NOT = {
                has_modifier = relic_activation_cooldown
            }
        }
    }
}
